import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Customize to your brand
    },
    secondary: {
      main: "#f50057",
    },
  },
});

export default theme;
