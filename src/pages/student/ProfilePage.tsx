import React from "react";

const ProfilePage: React.FC = () => (
  <div style={{ padding: 20 }}>
    <h2>My Profile</h2>
    <p>(Placeholder) Here the student sees their profile details.</p>
  </div>
);

export default ProfilePage;
